/** @deprecated Import from `@/lib/menu-helpers` instead. Re-export kept for existing paths. */
export {
  buildSoldOutSet,
  formatPrice,
  formatToday,
  getCategoryId,
  getCategoryName,
  groupItemsByCategory,
  isItemAddDisabled,
} from "../lib/menu-helpers.ts";
